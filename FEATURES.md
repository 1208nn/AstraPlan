# AstraPlan 功能详细说明

## 核心功能

### 1. AI 对话日程管理

#### 1.1 创建日程
**功能描述：** 通过自然语言创建日程

**示例输入：**
- "明天下午3点开会"
- "下周一上午9点到10点，地点在会议室A，主题是项目讨论"
- "本周五全天休假"
- "每周一到周五早上8点晨会"

**AI 自动补充：**
- 默认地点（如配置）
- 默认时长（如配置）
- 默认提醒（如配置）
- 时区转换

**防呆机制：**
- 检测重复日程
- 识别用户重复发送
- 时间冲突提醒

#### 1.2 修改日程
**示例输入：**
- "把今天的会议改到下午4点"
- "明天的会议地点改为线上"
- "取消本周五的会议"

**智能识别：**
- 模糊匹配事件
- 时间范围搜索
- 关键词匹配

#### 1.3 删除日程
**示例输入：**
- "删除今天的所有会议"
- "取消明天下午的约会"
- "清空本周的日程"

#### 1.4 查询日程
**示例输入：**
- "今天有什么安排？"
- "下周一到周五的日程"
- "查找包含'会议'的所有日程"

**返回格式：**
- 结构化列表
- 时间排序
- 详细信息

#### 1.5 图片识别
**支持类型：**
- 会议通知截图
- 日历截图
- 手写便签照片
- 邮件截图

**提取信息：**
- 会议主题
- 时间日期
- 地点
- 参与人员
- 其他备注

### 2. 用户配置

#### 2.1 提示词配置
**固定字段：**
- **默认地点：** 当 AI 无法确定地点时使用
- **默认时长：** 当用户未指定结束时间时使用
- **默认提醒：** 自动设置的提醒时间
- **时区：** 用于时间转换

**自定义说明：**
用户可添加额外上下文，如：
```
公司地址：浙江大学紫金港校区
工作时间：周一到周五 9:00-18:00
常用会议室：会议室 A、B、C
```

#### 2.2 AI 资源配置
**支持的 AI 服务商：**

| 服务商 | 配置项 | 说明 |
|--------|--------|------|
| OpenAI | API Key | 官方 API |
| Google AI | API Key | Gemini API |
| GitHub Models | API Key | 免费额度 |
| Cloudflare AI | API Key | Workers AI |
| ZJU AI | API Key | 浙大 AI 平台 |
| 自定义 | API Key + Base URL + Model | 任何 OpenAI 兼容 API |

**配置后优势：**
- 无限制使用次数
- 更快的响应速度
- 自定义模型选择

#### 2.3 微软账号管理
**绑定流程：**
1. 点击"绑定微软账号"
2. 跳转到微软登录页面
3. 授权日历访问权限
4. 自动同步到 Outlook

**权限说明：**
- Calendars.ReadWrite：读写日历
- User.Read：获取用户信息
- offline_access：刷新令牌

**解绑条件：**
- 必须先切换到 D1 数据源
- 解绑后日程保留在数据库中

#### 2.4 ICS 订阅
**使用场景：**
- 导入到其他日历应用（Apple 日历、Google 日历等）
- 与团队共享日程
- 备份日程数据

**订阅链接格式：**
```
https://your-domain.workers.dev/ics/YOUR-TOKEN
```

**费用：**
- D1 用户：免费
- MS 用户：消耗 20 额度

#### 2.5 数据源切换
**前提条件：**
- 已绑定微软账号
- 已配置自己的 AI Key

**可选数据源：**
- **微软日历 (ms)：** 日程同步到 Outlook，跨设备访问
- **D1 数据库 (d1)：** 独立存储，完全控制

**注意事项：**
- 切换前请确保数据已备份
- 两个数据源不自动同步
- 建议选择一个主要使用

#### 2.6 Passkey 管理
**添加 Passkey：**
1. 在设置页面点击"添加 Passkey"
2. 使用设备的生物识别（指纹/面容）
3. 设置 Passkey 名称
4. 保存

**使用 Passkey 登录：**
- 快速登录
- 无需输入密码
- 安全性更高

**管理 Passkey：**
- 查看所有 Passkey
- 删除不需要的 Passkey
- 支持多设备

### 3. 管理员功能

#### 3.1 创建邀请码
**参数：**
- 初始额度（默认 30）

**生成规则：**
- 格式：XXXX-XXXX-XXXX-XXXX
- 随机生成
- 唯一性保证

#### 3.2 管理邀请码
**操作：**
- 查看所有邀请码
- 查看使用情况（用户名、注册时间）
- 修改剩余额度
- 停用邀请码

**筛选条件：**
- 已使用/未使用
- 按注册时间排序
- 按剩余额度排序

#### 3.3 用户管理
**查看信息：**
- 用户名
- 剩余额度
- 数据源
- 注册时间
- 最后使用时间

**操作：**
- 调整额度
- 重置密码（待实现）
- 停用用户（待实现）

## 额度系统

### 额度消耗规则
| 操作 | 额度消耗 | 说明 |
|------|----------|------|
| 发送消息（无自备 AI） | 1 | 使用共享 AI 资源 |
| 发送消息（有自备 AI） | 0 | 使用自己的 AI Key |
| 启用 ICS（D1 用户） | 0 | 免费功能 |
| 启用 ICS（MS 用户） | 20 | 一次性消耗 |
| 其他操作 | 0 | 不消耗额度 |

### 获取额度方式
1. **注册时获得：**
   - 无 MS 账号：10 次
   - MS 账号：30 次

2. **管理员分配：**
   - 联系管理员增加额度

3. **配置自备 AI：**
   - 无限额度

## 数据安全

### 加密存储
- **密码：** SHA-256 哈希
- **AI API Key：** AES-GCM 加密
- **微软令牌：** AES-GCM 加密

### 会话管理
- **JWT 令牌：** HMAC-SHA256 签名
- **过期时间：** 24 小时
- **刷新机制：** 自动刷新

### 数据隔离
- 每个用户的数据完全隔离
- 管理员无法查看用户的聊天记录
- 聊天记录仅存储在浏览器本地

## 性能优化

### 前端优化
- PWA 离线缓存
- 懒加载
- 图片压缩上传

### 后端优化
- D1 数据库索引
- API 响应缓存
- 微软 Token 自动刷新

### AI 优化
- 上下文窗口控制（最近 10 条消息）
- 流式响应（待实现）
- 并发请求限制

## 扩展性

### 支持的日历格式
- iCalendar (.ics)
- Microsoft Graph API
- 自定义 JSON 格式

### 可集成的 AI 服务
- 任何 OpenAI 兼容 API
- Cloudflare Workers AI
- 自建 AI 服务

### 可扩展的功能
- 日程模板
- 重复日程规则
- 日程分类标签
- 团队协作
- 日程分享

## 使用技巧

### 1. 自然语言技巧
**时间表达：**
- "明天" "后天" "下周一"
- "3天后" "2周后"
- "本月最后一天"

**时长表达：**
- "1小时" "30分钟"
- "上午9点到11点"
- "全天"

**重复表达：**
- "每天" "每周" "每月"
- "每周一到周五"
- "每隔2天"

### 2. 配置技巧
**提示词设置：**
```
默认地点：紫金港校区
默认时长：60分钟
默认提醒：15分钟

额外说明：
- 工作日 9:00-18:00
- 午休时间 12:00-13:00
- 常用会议室：A101, B202
- 通勤时间：1小时
```

### 3. 图片识别技巧
**最佳实践：**
- 清晰的截图
- 包含完整信息
- 避免反光和模糊
- 横屏截图效果更好

## 故障排除

### 常见问题

**Q: AI 响应慢？**
- 检查网络连接
- 切换 AI 服务商
- 使用自备 API Key

**Q: 日程创建失败？**
- 检查时间格式
- 确认权限配置
- 查看错误提示

**Q: 微软同步失败？**
- 检查令牌是否过期
- 重新授权
- 确认网络连接

**Q: 额度不足？**
- 配置自备 AI Key
- 联系管理员增加额度
- 检查使用情况

---

更多问题请查看 [README.md](README.md) 或提交 Issue。
